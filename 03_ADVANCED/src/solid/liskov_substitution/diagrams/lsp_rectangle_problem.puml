@startuml lsp_rectangle_problem
!theme plain
skinparam classAttributeIconSize 0

title Naruszenie LSP - Problem Rectangle/Square

class Rectangle {
  # width: int
  # height: int
  --
  + setWidth(int): void
  + setHeight(int): void
  + getWidth(): int
  + getHeight(): int
  + getArea(): int
}

class Square extends Rectangle {
  + setWidth(int): void
  + setHeight(int): void
}

note right of Square::setWidth
  @Override
  public void setWidth(int width) {
      this.width = width;
      this.height = width; // ❌
  }

  ❌ PROBLEM: Zmienia oba wymiary!
  Narusza kontrakt Rectangle
end note

note bottom of Rectangle
  void testRectangle(Rectangle r) {
      r.setWidth(5);
      r.setHeight(4);
      assert r.getArea() == 20;
  }

  ✅ Dziala dla Rectangle
  ❌ FAIL dla Square (area = 16)
end note

note bottom of Square
  ❌ Naruszenie LSP:
  • Square nie jest podstawialny za Rectangle
  • Zmienia zachowanie metod z klasy bazowej
  • Kontrakt setWidth() jest zlamany
end note

@enduml

